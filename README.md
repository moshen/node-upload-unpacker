# upload-unpacker

## Setup

    npm install

## Default configuration

    {
      "port"       : 8080,
      "hostname"   "127.0.0.1",
      "endpoint"   "/",
      "dest"       __dirname + "/uploads/",
      "keyHeader"  "X-Upload-Key"
    }

## Run

    npm start

